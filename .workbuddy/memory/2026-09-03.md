# 2026-09-03 工作日志 — ITERATION 2 地址与网络资源管理交付

## 任务闭环
- **ITERATION 2 完成**：区域/小区/楼栋/单元/房间 + OLT/PON/ONU 台账 + 下单资源核查三态（RESOURCE_OK / RESOURCE_INSUFFICIENT / NO_COVERAGE）
- 提交：`5d5c74a`（功能 41 文件 +2110 行）、`6c9b69b`（验证脚本+报告+backlog）
- 任务 #6/#7/#8/#9/#10 全部完成

## 关键技术决定
- 8 张 net_* 表均 extends BaseDO（含 @Version version 列、@TableLogic deleted 列）——**建表必须带 version 列**，否则 MyBatis-Plus selectList 报 Unknown column 'version'
- 资源核查地址解析：逆向逐级匹配——小区（名称/注册地址最长命中，**len>0 才认可**，否则误选第一个小区）→ 楼栋 likeRight → 单元 likeRight/首单元兜底 → 房号精确
- matchCommunity 边界 bug 教训：`len > bestLen`（bestLen 初始 -1）导致全部未命中时误选第一个记录，须 `len > 0 && len > bestLen`
- 订单回写：OrderCreateReq/BroadbandOrder/OrderServiceImpl.create() 三处 + OrderVO 补 3 展示字段；详情返回嵌套 {order:{...}}，BeanUtils.copyProperties 同名字段自动带出
- check 权限 order:create，台账 list resource:view / create resource:edit（菜单 400/401/402 seed）

## 顺带修复的既有缺陷（验证脚本暴露）
1. WorkOrderTimelineMapper.xml `selectByOrderId`：SELECT t.<include/> 仅 id 带 t. 前缀导致 JOIN work_order 后 create_by 歧义 → 501/500。展开全部字段加前缀
2. net_* 表缺 version 列（见上）

## 环境 quirks（复用）
- 后端打包前必须停 8080 进程（netstat 找 PID → PowerShell Stop-Process），否则 repackage 无法 rename jar
- Vite build 清空 dist 时触沙箱批量删除保护（>50 文件）→ 先 mv dist dist_old_xxx 再 build；删除残留目录用 PowerShell 逐文件 Remove-Item
- PowerShell 工具禁止 cmd.exe；rmdir /s /q 会被安全策略拦截

## 验证记录
- verify-resource.cjs 19/19：三态 4 + 台账 list 8 + 新增房间幂等 + 订单创建/回写 3 + 权限隔离 2
- verify-datascope2.cjs 回归 9/9 不回归
- vue-tsc exit 0；vite build ✓ 13.75s
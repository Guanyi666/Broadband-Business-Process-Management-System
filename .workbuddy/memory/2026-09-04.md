# 2026-09-04 工作日志

## Dashboard 权限门控修复（b63a3f9，dev 分支）
- 需求：装维角色（有 dashboard:view 可进看板页）部分模块接口超权限 → 403 → 全局"加载失败"报错。
  期望：无权限看板直接隐藏，不发请求、不报错，页面其他内容正常。
- 权限矩阵（后端已确认）：
  - install1(INSTALLER) 有 dashboard:view/workorder:view/my 类 → overview/trend/duration/page 200
  - 无 dispatch:view / workorder:sla:view / installer:view / attendance:view-all / leave:approve*
    → dispatch/stats、expiring、online、on-duty、pending-approvals 全 403
- 修改点：
  1. utils/request.ts：接口级 403 静默 reject（去全局 ElMessage；页面级仍由路由守卫跳 403）
  2. composables/useDashboard.ts：模块级 requirePerm 门控 + isForbidden() 检测；
     counts 需 installer:view/attendance:view-all/leave:approve* 之一
  3. views/dashboard/index.vue：dispatch/sla/counts 三组面板 v-if 门控 + 动态列宽撑满
  4. api/dashboard.ts：注释同步
- 验证：verify-dashboard-perm.cjs 22/22（install1 无权限 5 接口 403、有权限 4 接口 200；
  disp1 9 接口全 200 回归；权限码快照一致）；vue-tsc 0 错误；vite build ✓ 16.23s
- 环境：docker-compose up（容器已在跑）→ 后端 java -jar 启动，前端未起 dev server（无需 UI 实操，后端权限矩阵已实证）

## 0904 汇报稿（docs/0904汇报.md，未提交）
- 面向项目经理的阶段性汇报（项目简介/总体进度/已完成工作/功能清单/验证/后续），
  内容按业务化语言组织，技术细节收敛
- 注意：该文件仍在工作区未 add/commit（用户未要求入库时保留为工作区文件）

## 环境备注
- 本机 git push 需代理 127.0.0.1:443 连 GitHub——当前代理不可达，推送失败（b63a3f9 待推送）
- PowerShell Remove-Item 递归删除有时静默失败，rm -rf 需沙箱放行；构建产物清理 OK

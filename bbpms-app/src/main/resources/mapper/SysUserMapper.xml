<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.bbpms.user.mapper.SysUserMapper">

    <!--
        SysUserMapper custom queries. Missing until this file was added:
        login (selectByUsername) and the auth-info lookups (roles / permissions /
        dataScope) all threw BindingException at runtime because no SQL was
        bound to these methods. map-underscore-to-camel-case handles the
        sys_user -> SysUser auto-mapping (phone_enc -> phoneEnc, etc.).
    -->

    <sql id="Base_Column_List">
        id, username, password, salt, nickname, real_name, avatar,
        phone, phone_enc, id_card_no_enc, gender, birthday, email,
        dept_id, user_type, status, tenant_id,
        last_login_ip, last_login_time,
        create_time, update_time, create_by, update_by, deleted, version
    </sql>

    <select id="selectByUsername" resultType="com.bbpms.user.entity.SysUser">
        SELECT <include refid="Base_Column_List"/>
        FROM sys_user
        WHERE username = #{username}
          AND deleted = 0
        LIMIT 1
    </select>

    <select id="selectUserRoles" resultType="java.lang.String">
        SELECT r.code
        FROM sys_role r
        JOIN sys_user_role ur ON r.id = ur.role_id
        WHERE ur.user_id = #{userId}
          AND r.deleted = 0
          AND r.status = 1
    </select>

    <select id="selectUserPermissions" resultType="java.lang.String">
        SELECT DISTINCT m.perms
        FROM sys_menu m
        JOIN sys_role_menu rm ON m.id = rm.menu_id
        JOIN sys_user_role ur ON ur.role_id = rm.role_id
        JOIN sys_role r ON r.id = ur.role_id AND r.deleted = 0 AND r.status = 1
        WHERE ur.user_id = #{userId}
          AND m.deleted = 0
          AND m.perms IS NOT NULL
          AND m.perms &lt;&gt; ''
    </select>

    <select id="selectUserDataScope" resultType="java.lang.Integer">
        SELECT MAX(r.data_scope)
        FROM sys_role r
        JOIN sys_user_role ur ON r.id = ur.role_id
        WHERE ur.user_id = #{userId}
          AND r.deleted = 0
          AND r.status = 1
    </select>

</mapper>

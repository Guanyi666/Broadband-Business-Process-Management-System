# Sprint A Issue Register

> Created during Phase 1 project audit. Only mark an item closed after a real verification pass.

| ID | Priority | Module | Issue | Root Cause | Proposed Fix | Status |
|----|----------|--------|-------|------------|--------------|--------|
| SA-P0-001 | P0 | Docker / DB / Redis | Docker environment is not verified in this workspace. `docker version` could not connect to `npipe:////./pipe/docker_engine`; Docker also reported access denied reading `C:\Users\TEMP\.docker\config.json`. | Docker daemon is unavailable or inaccessible from the current environment. | In Phase 2, start/enable Docker Desktop, then run `docker compose down -v && docker compose up -d` and verify MySQL, Redis, schema, and seed data. | **Closed (Phase 2)** — Docker Desktop started; `docker compose down -v && up -d`; 27 tables, seed data, 03-extensions all verified; `bbpms_app` connects. |
| SA-P0-002 | P0 | Runtime / E2E | Spring Boot startup, `/actuator/health`, `/doc.html`, login, and real DB/Redis smoke flow remain unverified. | P0 runtime validation depends on Docker MySQL/Redis, which is not currently available. | Complete Phase 2 first, then run backend startup and auth/core API smoke tests against the real services. | **Closed (Phase 3)** — app starts in ~10.8s, health UP, doc.html 200, 0 ERROR. |
| SA-P0-003 | P0 | Auth | **Login returns HTTP 500 for every account** — `TokenServiceImpl.issue` called `jwtUtils.getJti(refreshToken)` which parses the HS256 refresh token with the RSA access-token parser (`JwtUtils.parse`), throwing jjwt `UnsupportedJwtException` → unhandled 500. | Refresh token is HS256 (refresh secret), access token is RS256; wrong parser used. | Use `jwtUtils.getJti(jwtUtils.parseRefresh(refreshToken))` in `issue()`. | **Closed (Phase 4)** — fixed `TokenServiceImpl.java:47`; auth E2E 20/20 PASS after fix. |
| SA-P1-001 | P1 | Tests | No Spring Boot integration tests are present. `mvn verify` passes but only unit tests execute. | Test tree contains only plain unit tests and no `*IT.java` / `@SpringBootTest` tests; Failsafe has no active integration-test execution to run. | Add minimum `AuthIntegrationTest`, `OrderIntegrationTest`, and `WorkOrderIntegrationTest`, then bind/execute integration tests in Maven. | **Closed (Phase 9)** — 3 `@SpringBootTest` ITs added under `src/test/java/com/bbpms/integration/` (9 tests); Failsafe declared in `bbpms-app/pom.xml` (parent pluginManagement alone is inert) with `**/*IntegrationTest.java` include; Surefire excludes them. `mvn verify` = 49 tests / 0 failures (40 unit + 9 integration), incl. full core chain: create→audit→auto work order→dispatch→accept→start→install steps→complete(BSS)→COMPLETED+FINISHED. |
| SA-P1-002 | P1 | H5 / WorkOrder API | H5 expects `/work-orders/my` data as `{ list, total, hasMore }`, but backend returns `PageResp` with `{ records, total, pageNum, pageSize, pages }`. | Frontend pagination adapter is not aligned to backend `PageResp`; H5 also sends `page/size` while backend expects `pageNum/pageSize`. | Align H5 API/store to backend `PageResp`, or add a deliberate adapter layer. | **Closed (Phase 7)** — `api/workorder.ts` rewrote with PageResp→{list,total,hasMore} adapter; store sends `pageNum/pageSize`; H5 dev-server smoke PASS. |
| SA-P1-003 | P1 | Admin / WorkOrder API | Admin workorder APIs use `/workorders/*`, while backend exposes `/api/work-orders/*`; one admin function also hardcodes `/api/work-orders/page` despite Axios `baseURL=/api`. | Legacy frontend endpoint names survived the `/work-orders` contract update. | Normalize admin API paths to `/work-orders/*` without embedding `/api` in request URLs. | **Closed (Phase 8)** — `api/workorder.ts` rewrote to `/work-orders/*`; detail/list/dispatch-board/sla pages aligned; admin build PASS. |
| SA-P1-004 | P1 | Auth / H5 | H5 public-key API expects `{ publicKey }`, but backend `/api/auth/public-key` returns a raw string in `R.data`; H5 may fall back to plaintext password. | Frontend type and login code diverged from backend response shape. | Change H5 `fetchPublicKey` handling to accept the string response, or change backend response DTO consistently for both frontends. | **Closed (Phase 7)** — H5 `fetchPublicKey()` now returns the raw PEM string and login encrypts; login smoke PASS. |
| SA-P1-005 | P1 | Dispatch / Attendance | H5 clock-in writes `attendance:active`, but dispatch candidate loading reads `installers:active`. A normal H5 attendance flow may not make installers dispatchable. | Attendance and installer-location modules use different Redis online keys without a bridge. | Make clock-in/location update a single source of truth, or have dispatch consume the attendance active key consistently. | **Closed (Sprint B, SB-1)** — `installers:active` kept as the dispatch/online source of truth; `AttendanceServiceImpl.clockIn` now mirrors into it (`zAdd`), `clockOut` and `AttendanceScheduleJob.autoCheckoutStale` mirror `zRem`; constant promoted to `InstallerProfileServiceImpl.ONLINE_KEY` (public). Verified: repro before fix showed clock-in landed only in `attendance:active` (dispatch key empty); after fix a clocked-in installer (id=8, no location report) appears in `GET /dispatch/candidates` (`["8"]`); clock-out removes from both ZSETs; `mvn verify` 49/49 green. |
| SB-N1 | P1 | Attendance | **Same-day re-sign-in blocked**: after clock-in → clock-out, a second clock-in the same day hits the idempotency early-return (`existing.getClockInAt() != null`) and returns the stale `OFF_DUTY` record — installer cannot go back on duty. Discovered while verifying SB-1. | Idempotency guard does not check whether the day's record was already closed. | In `clockIn`, treat an existing record as done only when it has no `clockOutAt`; on re-sign-in reset `clockOutAt`/`workMinutes`/`breakMinutes`. | **Closed (Sprint B, SB-1)** — idempotency early-return now requires `clockOutAt == null`; re-sign-in resets `clockOutAt` (work/break minutes already zeroed). Verified: install2 OFF_DUTY → re-sign-in returns ON_DUTY with new `clockInAt`, `clockOutAt` cleared; `mvn verify` 49/49 green. |
| SA-P1-006 | P1 | Install / Events | Install completion calls `workOrderService.complete()` and then publishes `InstallCompletedEvent`; `OrderEventListener` handles that event by calling `workOrderService.complete()` again. | Event ownership is unclear: completion command and completion event both mutate the same work-order state. | Remove the duplicate state mutation listener or make the event listener idempotent/read-only. | **Closed (Phase 5)** — real failure form was SA-P0-007 (`UnexpectedRollbackException` from the duplicate flip); fixed by making `markFinished` idempotent. WorkOrder IT full chain (Phase 9) re-verifies: single COMPLETED + FINISHED, no rollback. |
| SA-P1-007 | P1 | Frontend Verification | Admin and H5 builds cannot currently be verified because `node_modules` is absent; `vue-tsc` is not found. | Dependencies were cleaned from the workspace and not reinstalled. | Run `cmd /c npm install` in each frontend before Phase 7/10 build verification. | **Closed (Phase 7)** — H5 `vue-tsc + vite build` PASS (8 TS errors fixed); admin `npm run build` PASS (Phase 8). |
| SA-P2-001 | P2 | Attendance | Monthly attendance summary is not calculated. | `att_attendance_summary` has schema and read path, but no rollup writer. | Sprint B: add scheduled/on-demand monthly aggregation after Sprint A closes. | **Closed (Sprint B, SB-6)** — lazy aggregation in `AttendanceServiceImpl.monthly()`: missing summary → `rollup()` aggregates `att_attendance_record` (SQL in `AttendanceRecordMapper.aggregateMonth`: total work minutes / signed-in days / late past 09:00+threshold / early leave before `bbpms.attendance.off-duty-time` default 17:00), absent days = elapsed month weekdays − work days, persists to `att_attendance_summary`, idempotent on re-read. Verified: seeded 2026-08 rows (08-03 540min, 08-04 09:30→16:40, 08-05 580min) → `workDays=3, totalWorkMinutes=1550, lateCount=1, earlyLeaveCount=1, absentCount=18` (21 weekdays−3), summary row persisted, second call returns persisted data. Related discovery **SB-N3**: `YEAR_MONTH` is a MySQL 8 reserved word — the read path (`AttendanceSummaryMapper.findByInstallerAndMonth`) and MyBatis-Plus generated SQL on `att_attendance_summary.year_month` threw 1064 on first real use; fixed with `@TableField("\`year_month\`")` + backticks in the annotation SQL (03-schema-extensions.sql also carries the DDL, and `break_start_at` was added for SA-P2-003). |
| SA-P2-002 | P2 | Leave | L2 approval order can bypass L1. | Approval service needs stricter level sequencing. | Sprint B: enforce current-level transition rules in `approve()`. | **Closed (Sprint B, SB-6)** — `LeaveServiceImpl.approve` now enforces strict level progression: the acting level must equal `currentLevel + 1` (reject 直接 L2 绕过 L1 with "审批必须按级别顺序进行（当前 L0，本次 L2）"), keeping the existing "已审批过" and requiredLevel checks. Verified: SICK (requiredLevel=2) direct L2 → 400 rejected; L1 → PENDING; L2 → APPROVED; repeat L2 → 400; L2 on a 1-level PERSONAL request → 400 "无需 L2 审批". |
| SA-P2-003 | P2 | Attendance | Break duration is fixed at 30 minutes per break end. | Attendance record lacks break start tracking in current implementation. | Sprint B: persist break start time and calculate elapsed duration. | **Closed (Sprint B, SB-6)** — new `att_attendance_record.break_start_at` column (idempotent `_add_col_if_missing` in 03-schema-extensions.sql + ALTER on the running DB), `AttendanceRecord.breakStartAt`, `AttendanceVO.breakStartAt`; `breakStart` records the timestamp, `breakEnd` settles real elapsed minutes (legacy rows without break_start_at fall back to 30), and clock-out while ON_BREAK settles the open break first so workMinutes is exact. Verified: ~65s real break → `breakMinutes=1` (not 30); double break-end rejected; ON_BREAK clock-out settles break; break/start on OFF_DUTY rejected. |
| SA-P1-010 | P1 | DataScope / Auth | **`GET /api/work-orders/my` returns HTTP 500 (NPE)** — `DataScopeAspect.before` dereferenced `anno.value()` with `anno == null`. | Spring AOP matched the `@annotation(anno) || @within(anno)` disjunction against the MyBatis Mapper method `WorkOrderMapper.selectPageWithScope` (Mapper beans pass through Spring auto-proxying; method carries `@DataScope`) but **failed to bind the annotation parameter** (JDK-proxy Mapper invocation) → `anno == null` → NPE at the very first `log.debug` line. | Null-guard in `DataScopeAspect.before` (log + skip when `anno == null`; actual SQL filtering is done by `DataScopeInnerInterceptor` at the MyBatis layer, unaffected). | **Closed (Phase 4)** — guard added at `DataScopeAspect.java:23-29`; joinpoint debug log now confirms the matched method; `403_check.mjs` 5/5 PASS including `install1 GET /work-orders/my → 200`. |
| SA-P1-011 | P1 | Auth / Errors | **Malformed/expired token → HTTP 500** instead of 401. `JwtUtils.parse` threw `IllegalArgumentException`, landing in the generic handler. | jjwt parse failure was rethrown as `IllegalArgumentException` (an unhandled exception class). | Throw `BizException(ResultCode.TOKEN_INVALID)` from `parse()`/`parseRefresh()`; map 1010/1011 to HTTP 401 in `GlobalExceptionHandler.handleBiz`. | **Closed (Phase 4)** — both files fixed; bad-token E2E asserts HTTP 401. |
| SA-P1-012 | P1 | Auth / Errors | **Missing/invalid token returned HTTP 200 with body code 401** — frontends branching on HTTP status (H5 redirects on 401) see a success response. | `GlobalExceptionHandler.handleBiz` always returned HTTP 200. | Return `ResponseEntity` mapping 401/1010/1011→HTTP 401, 403→HTTP 403, login business failures (1001/1002/1003)→HTTP 200. | **Closed (Phase 4)** — `handleBiz` now returns `ResponseEntity<R<Void>>`; no-token E2E asserts HTTP 401. |
| SA-P1-013 | P1 | Tests / 403 semantics | `403_check.mjs` `cs1 POST /orders/999/audit` with an **empty body returned 400, not 403**, looking like a missing `@PreAuthorize`. | Spring MVC resolves and validates `@RequestBody` (→ 400) **before** the method-invocation AOP proxy runs `@PreAuthorize` (→ 403). Test design flaw, not an app bug. | Send a valid body `{pass:true, remark:"…"}` so the permission check is what rejects the call. | **Closed (Phase 4)** — script corrected; cs1 audit → HTTP 403 confirmed (`order:audit` missing for CUSTOMER_SERVICE). |
| SA-P0-004 | P0 | Order / Crypto | **Customer creation returns 500** — `CryptoUtils.sm4Encrypt` threw `IllegalStateException` for every PII write. | `OrderProperties.sm4Key` default `"bbpms-app-sm4-key-CHANGE-ME"` is not 32 hex chars (SM4 needs a 16-byte key) — `hexToBytes` produced garbage → engine init failed. | Default the key to a valid SM3-derived 32-hex value; document the 32-hex contract. | **Closed (Phase 5)** — `OrderProperties.java:30-39`; core E2E customer create PASS. |
| SA-P0-005 | P0 | DB / Order | **Customer creation returns 500** after SM4 fix — `Unknown column 'status'` in `CustomerMapper.xml` SELECT. | `Customer` entity has `status`; `customer` DDL lacks the column. | Add `status` via the idempotent `_add_col_if_missing` pattern in `03-schema-extensions.sql`. | **Closed (Phase 5)** — column added; `docker compose down -v && up` verified; core E2E PASS. |
| SA-P0-006 | P0 | User / Dispatch | **`POST /api/installers/location` returns 500** — `Unknown column 'id' in 'where clause'`. | `InstallerProfile extends BaseDO` (PK annotated `id`) but the `installer_profile` table PK is `user_id`; `updateById`/`save` generated `WHERE id=?`/INSERT `id` SQL. | Stop extending BaseDO; declare `@TableId(value="user_id", type=IdType.INPUT)` plus the audit/`@TableLogic`/`@Version` fields on the entity itself. | **Closed (Phase 5)** — `InstallerProfile.java` rewritten; location update + dispatch PASS. |
| SA-P0-007 | P0 | Order / Install / TX | **`POST /api/install/{id}/complete` returns 500** — `UnexpectedRollbackException`. | Two completion paths flip the parent order: `WorkOrderServiceImpl.complete` mirrors it via `updateStatus` (→ FINISHED), then `InstallServiceImpl.complete` calls `markFinished` again — second call throws (`FINISHED` not in {DISPATCHED, INSTALLING}), which marks the outer TX rollback-only; the catch swallows it; commit explodes. Real form of SA-P1-006. | Make `markFinished` idempotent: return early when already FINISHED. | **Closed (Phase 5)** — `OrderServiceImpl.java` guard; complete E2E PASS (work order COMPLETED + order FINISHED). |
| SA-P0-008 | P0 | Order / Customer API | **`GET /customers/page` returned empty records** — `CustomerController.page` was a placeholder that hardcoded an empty page; admin customer list always empty. | Controller did not delegate to a real service pagination. | Delegate to `CustomerService.page` (real MP pagination + mask). | **Closed (Phase 8)** — service `page()` implemented; `phase8_contract.mjs` asserts `total>0` and `records.length>0` PASS. |
| SA-P0-009 | P0 | Order / DataScope | **`GET /orders/page` returns HTTP 500** — `SQLSyntaxErrorException near 'ORDER BY o.create_time DESC LIMIT 5'`; every admin orders page request failed. | `BroadbandOrderMapper.xml#selectPageWithScope` hardcoded `ORDER BY o.create_time DESC` while the caller's wrapper (`${ew.sqlSegment}`) appended its own `ORDER BY` → duplicate ORDER BY. Only this XML uses `${ew.sqlSegment}`; the other 5 scoped statements use named params and were unaffected. | Remove the hardcoded ORDER BY from the XML; ordering belongs to the caller's wrapper (which also serves `OrderQueryReq.orderBy`). | **Closed (Phase 8)** — XML comment updated; restart verified; 7/7 page endpoints smoke PASS; contract 8/8 PASS. |
| SA-P1-014 | P1 | Dispatch / Leave | Leave-candidate exclusion could not be exercised via the API in tests: `leave/apply` enforces 4h advance notice, so an API-created leave is never "currently active". | Business rule by design; only the filter is testable. | Seed an active APPROVED leave row directly in MySQL for the linkage test (core_e2e / leave_sla_e2e). | **Closed (Phase 5)** — `leave_sla_e2e.mjs` scenario A PASS (install2 excluded from candidates). |
| SA-P1-015 | P1 | H5 / WorkOrder | H5 work-order actions (accept / start) were missing from the API layer. | `api/workorder.ts` only had list/detail; no accept/start methods. | Add `acceptWorkOrder` / `startWorkOrder` mapped to `POST /work-orders/{id}/accept` / `start`. | **Closed (Phase 7)** — H5 build PASS; dev-server flow smoke PASS. |
| SA-P1-016 | P1 | H5 / Upload | H5 installation photo upload was not wired. | No upload call existed in the H5 flow. | Wire upload into the complete/photo step. | **Closed (Phase 7)** — H5 build PASS. |
| SA-P1-017 | P1 | Admin / API 契约 | 8 admin API files misaligned with backend paths/field names: dispatch endpoint formats, file presign semantics, log/install paths, user change-password path+fields, role assignMenus method+body, installer singular/plural+field mapping, candidates field mapping, `PageResp`→`PageResult` adapters. | Frontend calls and backend controllers diverged; no adapter layer. | Rewrite the 8 API modules with correct paths and explicit adapters; align affected pages (installer list/profile, profile/index, sla/Expiring, attendance/Report, file/index). | **Closed (Phase 8)** — admin `npm run build` PASS; `phase8_contract.mjs` 8/8 PASS. |
| SA-P1-018 | P1 | Admin / Dispatch board | Dispatch-board candidate fields misaligned (`score`→`totalScore`, `distanceKm`→`distance`). | CandidateDTO field names differ from the frontend `DispatchCandidate` type. | Map in `api/dispatch.ts` `dispatchCandidates`. | **Closed (Phase 8)** — admin build + contract PASS. |
| SA-P1-019 | P1 | Backend / Endpoints | 5 backend endpoints referenced by admin were missing: `GET /installers/page`, `GET /installers/locations`, `GET /installers/{id}/profile`, `GET /customers/search`, `GET /roles/page`. | Controllers had no matching methods (customers/page was a placeholder, see SA-P0-008). | Add service methods + controller endpoints; `PageResp`/mask consistent with existing conventions. | **Closed (Phase 8)** — all 5 smoke-tested in `phase8_contract.mjs` PASS. |
| SA-P2-004 | P2 | Dispatch | Concurrent `POST /dispatch/auto` on the same order: both HTTP calls return success but only one `dispatch_record` row lands (observed in Phase 6). De-dup mechanism is unclear — may be `WorkOrderService.create` idempotency or lock serialization; response semantics should be audited. | Response contract vs. side effects are not aligned under concurrency. | Audit `WorkOrderServiceImpl.create` idempotent path and decide: return 409 on second dispatch, or document last-write-wins. | **Closed (Sprint B, SB-4)** — root cause was a race, worse than observed: `OrderAuditedEvent` was consumed by TWO async listeners (this `OrderEventListener.onOrderAudited` created a bare work order with null installer; `OrderAuditedListener` created a complete one), and the three concurrent executors (listener + 2 manual calls) each passed the `selectByOrderId` check in the same commit window → 4 work orders + 3 records (reproduced). Fixes: (1) removed the bare-WO creator from `OrderEventListener` — autoDispatch owns creation; (2) `autoDispatch`/`manualDispatch` take an order-scoped Redisson lock `lock:dispatch-order:{orderId}` BEFORE the first SELECT (so the REPEATABLE_READ snapshot is created inside the lock), released in `afterCompletion` (post-commit, not at method exit — a stale snapshot inside the lock was the second hidden race); (3) idempotent short-circuit: a non-terminal existing WO is returned with 200 + same workOrderId instead of creating another WO/record. Semantics: **idempotent last-write-wins** (both calls 200, same WO id, single record), no 409 needed. Verified: concurrent pair → same workOrderId in both responses; DB has exactly 1 WO + 1 record; also with listener-auto-dispatch racing the two manual calls (1 WO + 1 record). Related discovery **SB-N2** (re-sign-in left a stale clock_out_at → clock-out early-return forever) fixed in `AttendanceServiceImpl.clockIn` re-sign-in branch with an explicit `LambdaUpdateWrapper` `.set(clockOutAt, null)`. |
| SB-N2 | P1 | Attendance | **Re-sign-in left a stale `clock_out_at`** (SB-N1 fix regression): `rec.setClockOutAt(null)` was skipped by MyBatis-Plus default update strategy (null fields are not written), so after re-clock-in the day's record still had an old clock-out time → `clockOut()` early-returned forever (installer could never clock out again, stayed online in `installers:active`, kept getting dispatched). Discovered when installers 6/8 could not be clocked out during SB-4 verification (their records showed `clock_out_at` earlier than `clock_in_at`). | `updateById` skips null fields; the null-clear must go through an explicit UPDATE. | In the re-sign-in branch, clear `clock_out_at` via `LambdaUpdateWrapper.set(clockOutAt, null)` after `updateById`. | **Closed (Sprint B, SB-4)** — explicit wrapper UPDATE added in `AttendanceServiceImpl.clockIn`; full cycle verified: clock-in → clock-out → re-clock-in (`clockOutAt` undefined again) → clock-out → OFF_DUTY. |
| SA-P2-005 | P2 | Admin / Dashboard | Dashboard / monitor pages are missing or non-functional in admin. | Not implemented in this tree. | Sprint B: build on the SLA + attendance data already exposed. | Deferred (Sprint B) |
| SA-P2-006 | P2 | Order / SQL | `OrderQueryReq.orderBy` is interpolated into the wrapper ORDER BY without whitelist — SQL-injection surface (only `orders/page`). | No column whitelist on the sort field. | Sprint B: whitelist `orderBy` to known columns + direction enum. | **Closed (Sprint B, SB-3)** — `OrderServiceImpl.sanitizeOrderBy` whitelists real `broadband_order` columns (`create_time/update_time/order_no/status/expected_install_date/appointment_time`) and requires `asc|desc`; anything else → 400 非法的排序字段. Verified: `create_time desc`/`status asc` → 200; `create_time desc; DROP TABLE broadband_order` / `create_time` / `password desc` → HTTP 200 body code 400 rejected. |
| SA-P2-007 | P2 | Auth / Logging | **`login_log` is always empty** — `logs/login/page` total=0 despite many successful logins. `LoginLogListener` (log module) subscribes to `BbpmsEvents.LoginLogEvent`, but `AuthServiceImpl` never publishes it — dead code. | Publish call missing from the login success/failure paths. | Sprint B: publish `LoginLogEvent` in `AuthServiceImpl.login` (success + failure), keep listener as-is. | **Closed (Sprint B, SB-2)** — `AuthServiceImpl.login` now publishes `LoginLogEvent` on success (status=1) and all failure branches (user missing / decrypt failed / wrong password / disabled, status=0) via `publishLoginLog` helper; listener untouched. Verified: login_log 0→N rows; `logs/login/page` total=4 showing 登录成功 / 密码错误 / 用户不存在; `mvn verify` 49/49 green. |
| SA-P2-008 | P2 | Tests / e2e | `leave_sla_e2e.mjs` scenario B ("SLA accept-timeout auto-cancel") always fails: script comments claim timeout=1min and polls 180s, but the actual config is `bbpms.workorder.sla.accept-timeout-minutes: 30` — the poll can never observe a cancel. | Script/comment vs config drift, not an app bug. | Sprint B: let the script pass a lowered timeout via env/config override, or backdate `dispatch_time` in SQL like the manual verification does. | **Closed (Sprint B, SB-5)** — scenario B now backdates `dispatch_time` 31min via SQL (the approach approved in the fix plan; no app restart / config change needed) and polls ≤100s for the next scan (interval 30s + initialDelay 30s). Engine unchanged. Verified: `leave_sla_e2e.mjs` 7/7 PASS including "SLA auto-cancelled unaccepted work order status=AUTO_CANCELLED". |
| SB-N3 | P2 | Attendance / SQL | `att_attendance_summary.year_month` is a MySQL 8 reserved word (`YEAR_MONTH`): the pre-existing read path `AttendanceSummaryMapper.findByInstallerAndMonth` threw 1064 on first real call, and MyBatis-Plus generated INSERT/UPDATE SQL on the entity would throw too. | Column named after a reserved word without backticks; the bug surfaced only when SA-P2-001 made the path reachable. | Backtick every reference: `@TableField("\`year_month\`")` on the entity + backticks in annotation SQL. | **Closed (Sprint B, SB-6)** — fixed as part of SA-P2-001 (see above); verified by the same monthly-rollup assertions. |
| SB-N4 | P2 | DataScope / Dispatch | Dispatchers could not see AUTO dispatch records: 89/117 rows have `create_by IS NULL` because `autoDispatch` runs in an async listener without SecurityContext, and the DataScope SELF filter (`create_by = userId`) excluded them — the audit trail for system dispatches was invisible to everyone with SELF scope (disp1 调度员). | System-created rows carry no creator; strict SELF equality drops them. | `DataScopeInnerInterceptor` SELF clause becomes `(create_by = X OR create_by IS NULL)` — system rows belong to nobody and are visible to everyone within scope; applies to all 5 whitelisted statements (dispatch/workorder/order/install/user). | **Closed (Sprint B, SB-4)** — verified: `/dispatch/records/page` (disp1) returns the newest AUTO record first; `core_e2e.mjs` dispatch_record assertion green (was failing on the newest record missing from page 1). |
| SA-P3-001 | P3 | DataScope | DEPT/CUSTOM scopes are not implemented. | Business tables do not carry dept columns; interceptor intentionally skips unsupported scopes. | Later architecture work: add dept ownership model and SQL predicates. | Deferred |
| SA-P3-002 | P3 | SLA | `wo_sla_policy` is seeded but not used by the SLA scheduler. | Scheduler reads YAML properties instead of DB policy table. | Later enhancement: policy lookup/cache by business type. | Deferred |

